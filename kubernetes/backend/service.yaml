apiVersion: v1
kind: Service
metadata:
  name: backend-service          # must be exactly this — frontend/admin nginx already proxy here
  namespace: akash-shopnow
spec:
  selector:
    app: backend
  ports:
    - port: 5000
      targetPort: 5000
